<nav class="pagination">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}"><span aria-hidden="true">&larr;</span> Newer Articles</a>
    {{/if}}
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Older Articles <span aria-hidden="true">&rarr;</span></a>
    {{/if}}
</nav>
